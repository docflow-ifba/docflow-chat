name: docflow
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  flutter_vector_icons: ^2.0.0
  http: ^0.13.6
  provider: ^6.1.0
  json_serializable: ^6.6.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.3
  intl: ^0.18.1
  flutter_animate: ^4.1.1+1
  build_runner: ^2.4.5

flutter_icons:
  android: true
  ios: true
  image_path: "assets/logo/logo-alt.png"

flutter:
  uses-material-design: true
  assets:
    - assets/images/